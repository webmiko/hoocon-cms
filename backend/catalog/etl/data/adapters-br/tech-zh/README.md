# OEM tech drawings (Chinese) — geometry source for RU rebuild

PNG renders of plant sheets (A4 @ ~2×). Used by
`catalog.etl.br_adapter_tech_sheets` to build Russian PDFs under
`_инструкции-pdf/RU/` (Chinese title blocks blanked, RU header/requirements).

Do not attach these PNG as ProductFile — only the generated RU PDFs.
