"""Палитра Unfold primary из brand hex проекта.

Пример: Hoocon brand ``#dc1313``. Перед продом пересчитать OKLCH
конвертером (L/C/H) и при необходимости подправить chroma.

Не хранить здесь секреты — только публичные цвета бренда.
"""

from __future__ import annotations

# Hoocon red #dc1313 — опорная шкала (OKLCH). Уточнять визуально в UI.
PRIMARY_OKLCH: dict[str, str] = {
    "50": "oklch(97.5% 0.015 25)",
    "100": "oklch(94.0% 0.035 25)",
    "200": "oklch(88.0% 0.070 25)",
    "300": "oklch(78.0% 0.120 25)",
    "400": "oklch(68.0% 0.170 25)",
    "500": "oklch(55.0% 0.210 25)",
    "600": "oklch(48.0% 0.200 25)",
    "700": "oklch(40.0% 0.170 25)",
    "800": "oklch(33.0% 0.130 25)",
    "900": "oklch(28.0% 0.100 25)",
    "950": "oklch(20.0% 0.070 25)",
}

# Подстановка под другой проект:
# 1) взять brand hex из токенов;
# 2) hex → OKLCH, зафиксировать hue H;
# 3) собрать 50..950 с тем же H;
# 4) проверить контраст кнопок в light-теме.


def primary_from_brand_hex(_hex_color: str) -> dict[str, str]:
    """Вернуть шкалу primary для Unfold.

    Args:
        _hex_color: Brand hex вида ``#dc1313`` (зарезервировано под
            автоматический конвертер; сейчас возвращает канон Hoocon).

    Returns:
        Словарь оттенков ``50``…``950`` в OKLCH для ``UNFOLD["COLORS"]``.
    """
    # В каноне — явная таблица. Автоконвертер (hex→oklch) можно добавить
    # в проектный utils, не втягивая тяжёлые зависимости в БЗ.
    return dict(PRIMARY_OKLCH)
