/**
 * Brand palette for JS (PWA manifest, theme-color meta).
 *
 * Keep in sync with ``tokens.css`` ``:root`` brand tokens (logo.svg #da0e2b).
 * CSS remains the runtime source for components; this module is for build/meta only.
 */

/** Primary brand fill — ``--color-brand`` / logo (light). */
export const COLOR_BRAND = "#da0e2b";

/** Brand hover — ``--color-brand-hover`` (light). */
export const COLOR_BRAND_HOVER = "#b80c24";

/** Label on brand fill (light) — ``--color-on-brand``. */
export const COLOR_ON_BRAND = "#ffffff";

/** Page background — ``--color-bg`` (light). */
export const COLOR_BG = "#f3f4f7";
